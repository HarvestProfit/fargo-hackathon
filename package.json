{
  "name": "fargo-hackathon",
  "version": "1.0.0",
  "repository": "https://github.com/HarvestProfit/fargo-hackathon",
  "author": "Jaryd Krishnan <jaryd@harvestprofit.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "db:setup": "cd backend && bundle exec rails db:setup",
    "db:migrate": "cd backend && bundle exec rails db:migrate",
    "lint": "yarn --cwd=./frontend lint && cd backend && bundle exec rubocop --config='.rubocop.yml'",
    "postinstall": "cd backend && bundle install",
    "start": "foreman start"
  },
  "workspaces": [
    "frontend"
  ],
  "devDependencies": {
    "foreman": "^3.0.1"
  }
}
